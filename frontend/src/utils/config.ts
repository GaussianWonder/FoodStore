// Never do this btw

export interface Config {
  unsplash: {
    accessKey: null;
    secretKey: null;
  };
}

const config: Config = {
  unsplash: {
    accessKey: null,
    secretKey: null,
  },
};

export default config;
